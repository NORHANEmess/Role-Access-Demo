# Secret key for signing cookies and managing session security
SESSION_SECRET=my_secret_key

# Port the server will run on (optional, defaults to 3000 if not set)
PORT=3000

# Database configuration (add this if you want to use a database in the future)
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name
